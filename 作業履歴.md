# 作業履歴

## 2025-11-01

### スマホ版でbook-cardの縦横比が連動しない問題を修正

**問題：**
- PC版では画像の縦横比を元にclass="book-card"の縦横比が連動しているが、スマホ版が連動していない

**修正内容：**
- `index.html`の`adjustBookCardHeights()`関数を改善
  - `img.naturalWidth > 0`と`img.naturalHeight > 0`のチェックを追加
  - サイズ設定のロジックを`setCardSize`関数に切り出して重複を削除
  - 画像が既に読み込まれている場合と、まだ読み込まれていない場合の両方に対応

**変更ファイル：**
- `index.html` (256-291行目)

**備考：**
- スマホ（画面幅768px以下）では3冊ずつ、PCでは10冊ずつ表示
- 画像のnaturalWidth/Heightが0の場合にサイズ設定をスキップすることで、画像読み込みエラー時の問題を回避

---

### register.htmlでページローディングが終わらないエラーを修正

**エラー内容：**
- `register.html?id=1:99 Uncaught TypeError: Cannot set properties of null (setting 'textContent')`
- 編集モードでページが無限ローディング状態になる

**原因：**
- 99行目で `document.getElementById('page-title')` を参照しているが、HTML内に該当する要素が存在しない

**修正内容：**
1. `register.html` 29行目に `<h1 id="page-title">` 要素を追加
   - 新規登録時：「書籍を登録」を表示
   - 編集時：「書籍を編集」を表示（99行目のJavaScriptで変更）
2. meta タグの非推奨警告に対応
   - `<meta name="mobile-web-app-capable" content="yes">` を追加（7行目）

**変更ファイル：**
- `register.html`

**備考：**
- Service Workerのキャッシュエラー（chrome-extension）は開発者ツールの拡張機能によるもので、実運用では問題なし
- faviconのリソースサイズエラーは別途確認が必要
